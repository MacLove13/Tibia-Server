import { MoveData, Rotation } from '@utils/interface/movementation';

export interface Character {
  // Move(data: MoveData);
  // MoveDir(rot: Rotation);
  // GetJSON();
  // GetID(): string;
  // Dispose();
  // SelfAnnouce();
  // Target(char: Character);
  // Untarget();
  // GetHP(): number;
  // Hit(dmg: number): { Exp: number };
  // Kill();
  // IsDead(): boolean
  // CanMove(): boolean
  // CanAttack(): boolean
  // AttackTarget();
}
